//문자형 변수를 한 개 선언 후, ‘A’ 값을 입력하고, 이를 이용하여 A부터 Z까지 출력하도록 하자. 문자형은 char로 선언하고, 문자 출력 시 서식문자는 %c로 한다.

#include <stdio.h>

void main()
{
    for (char a = 'A'; a < 'Z' + 1; a++) // 문자는 내부적으로 정수로 표현된다. 아스키코드를 참고하면 65 ~ 90까지임
    {
        printf("%c", a);
    }

}
