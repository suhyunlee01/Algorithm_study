//반복문을 사용하여 1부터 사용자가 입력한 정수까지의 합을 출력하는 프로그램을 작성하라. 

#include <stdio.h>


void main() {
    int a = 1;
    int sum = 0;
    for (int i = 0; i < a; i++) {
        printf("1 이상의 수를 입력하세요: \n");
        scanf_s("%d", &a);
        sum = sum + a;
        printf("지금까지 수의 합은 %d 입니다.\n", sum);
    }
}
